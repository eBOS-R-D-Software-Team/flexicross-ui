import React from 'react';
import { Layout } from 'antd';

const { Header } = Layout;

const AppHeader: React.FC = () => {
   return (
      <Header style={{ padding: 0, background:'#3a7aa6', height:60, display: 'flex' }}>        
      </Header>
   )
};

export default AppHeader;
